enabled: true
minify: true
container_fix: false               # legacy fallback; the trilbymedia engine fork ships container natively
auto_compile_on_save: false        # recompile when theme config is saved
scan_extensions: [twig, md, yaml, yml, php, html, htm, js]
