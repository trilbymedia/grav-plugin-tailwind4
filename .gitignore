.DS_Store
.idea/*
.vscode/*

# A Grav plugin ships self-contained: GPM/zip installs have no composer step,
# so vendor/ and composer.lock are committed (production-only, regenerated with
# `composer install --no-dev`) as part of cutting a release.
.phpunit.cache/

# Internal development plan, not published
PLAN.md
